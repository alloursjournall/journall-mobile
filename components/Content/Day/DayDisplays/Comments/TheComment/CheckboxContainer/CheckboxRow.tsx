// <>
import { View, Dimensions, Text, TextInput, Image, StyleSheet, TouchableOpacity } from 'react-native';

interface CheckboxRowProps {
    text: string|null,
    icon: string|null,
    handleCheck: any,
    handleCheckKey: string   
}

const CheckboxRow: React.FC<CheckboxRowProps> = () => {
    return (
        <View>
            
        </View>
    )
}